<form method="post" action="{{ path('repetition_maximum_delete', {'id': repetition_maximum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repetition_maximum.id) }}">
    <button class="btn">Delete</button>
</form>